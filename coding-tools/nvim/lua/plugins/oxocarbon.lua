return {
  "nyoom-engineering/oxocarbon.nvim",
  lazy = false,
  priority = 1000,
  -- oxocarbon has no native `transparent` option; lua/config/transparency.lua
  -- handles it by overriding the core background highlight groups.
}
